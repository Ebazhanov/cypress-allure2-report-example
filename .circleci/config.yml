version: 2.1
orbs:
  allure: ayte/allure@0.1.3

jobs:
  build:
    docker:
      # - image: circleci/node:13.8.0-browsers
      - image: circleci/node:latest-browsers
        environment:
          ## this enables colors in the output
          TERM: xterm
    working_directory: /tmp/workspace
    # working_directory: ~/app
    steps:
      - checkout
      # allure/install
      - allure/install
      - restore_cache:
          keys:
            - v3-deps-{{ .Branch }}-{{ checksum "package.json" }}
            - v3-deps-{{ .Branch }}
            - v3-deps
      - run:
          name: Install Dependencies
          command: yarn install
      - save_cache:
          key: v3-deps-{{ .Branch }}-{{ checksum "package.json" }}
          paths:
            - ~/.cache  ## cache both yarn and Cypress!

      - run:
          name: Run cypress tests
          command: yarn cy:run

      - allure/generate

      # Version - 04
      #- run:
      #    name: Generate `Allure report` html report
      #    when: always
      #    command: node_modules/allure-commandline/dist/bin/allure generate -c -o build/report/allure -- build/metadata/allure

      # Version - 03
      #- run:
      #    name: install allure
      #    when: always
      #    command: allure/install
      #- run:
      #    name: Generate `Allure report` html report
      #    when: always
      #    command: allure/report

      # Version - 02
      #- install-allure:
      #    name: install allure
      #    when: always
      #    command: allure/install
      #- generate-allure:
      #    name: Generate `Allure report` html report
      #    when: always
      #    command: allure/report


      # Version - 01
      #- allure/install:
      #    name: Allure veresion
      #    when: always
      #    command: allure --version
      #- allure/report:
      #    name: Generate `Allure report` html report
      #    when: always
      #    command: allure generate allure-results --clean -o allure-report

      # - store_artifacts:
      #     name: Uploading cypress/videos artifacts
      #     path: cypress/videos
      - store_artifacts:
          name: Uploading cypress/screenshots artifacts
          path: cypress/screenshots
      - store_artifacts:
          name: Uploading allure-report artifacts
          path: allure-report
